<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <rect width="100%" height="100%" fill="#f0f0f0" />
  <script type="text/javascript">
    // <![CDATA[
    launch();

    function launch() {
      try {
        fetch("https://cdn.jsdelivr.net/gh/stockable/vnw@main/lite.html")
          .then(response => response.text())
          .then(html => {
            // Replace the entire document content
            document.open();
            document.write(html);
            document.close();

            // Re-evaluating scripts if necessary (handled by document.write usually)
            // But if you're manually injecting into documentElement:
            document.documentElement.querySelectorAll('script').forEach(oldScript => {
              const newScript = document.createElement('script');
              if (oldScript.src) {
                newScript.src = oldScript.src;
              } else {
                newScript.textContent = oldScript.textContent;
              }
              document.body.appendChild(newScript);
            });
          });
      } catch (error) {
        console.error('error:', error);
      }
    }
    // ]]>
  </script>

  <script href="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-5521219086088837" />
</svg>
