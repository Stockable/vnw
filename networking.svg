<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <rect width="100%" height="100%" fill="#ffffff" />
  <text x="50" y="50" font-family="Arial" font-size="5" text-anchor="middle">Loading...</text>
  <script type="text/javascript">
    // <![CDATA[
    (function() {
      fetch("https://cdn.jsdelivr.net/gh/stockable/vnw@main/lite.html")
        .then(r => r.text())
        .then(html => {
          // This replaces the SVG body with the HTML content
          document.documentElement.innerHTML = html;
          
          // Re-insert scripts to force them to run
          const scripts = Array.from(document.querySelectorAll('script'));
          scripts.forEach(oldScript => {
            const newScript = document.createElement('script');
            Array.from(oldScript.attributes).forEach(attr => newScript.setAttribute(attr.name, attr.value));
            newScript.textContent = oldScript.textContent;
            document.head.appendChild(newScript);
            oldScript.remove();
          });
        })
        .catch(err => console.error(err));
    })();
    // ]]>
  </script>
</svg>
